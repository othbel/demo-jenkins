pipeline {
    agent any
    stages {
        stage('Build') {
            sh 'echo "Build stage, branch: main"'
        }
        stage('Test') {
            sh 'echo "Test stage, branch: main"'
        }
        stage('Deploy') {
            sh 'echo "Deploy stage, branch: main"'
        }
    }
}