pipeline {
    agent any
    stages {
        stage('Build') {
            sh 'echo "Build stage, branch: dev"'
        }
        stage('Test') {
            sh 'echo "Test stage, branch: dev"'
        }
        stage('Deploy') {
            sh 'echo "Deploy stage, branch: dev"'
        }
    }
}